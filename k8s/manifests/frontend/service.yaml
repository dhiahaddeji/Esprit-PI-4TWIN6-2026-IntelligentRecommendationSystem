apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: assurreco
spec:
  selector:
    app: frontend
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080   # Accessible via <NodeIP>:30080
