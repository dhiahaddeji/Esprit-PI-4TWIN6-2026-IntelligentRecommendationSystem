apiVersion: v1
kind: Service
metadata:
  name: alertmanager
  namespace: assurreco
  labels:
    app: alertmanager
spec:
  type: NodePort
  selector:
    app: alertmanager
  ports:
    - port: 9093
      targetPort: 9093
      nodePort: 30093
      name: http
